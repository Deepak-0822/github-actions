name: ESLint Check

on: [push, pull_request]

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run ESLint Custom Action
        uses: ./.github/actions/eslint-check
        # with:
        #   target: './src'
