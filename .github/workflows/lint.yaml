name: ESLint Check

on:
  push:
    branches: [feature/lint0.2]
  # pull_request:
  #   branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run ESLint Docker Action
      uses: ./actions/eslint-action
